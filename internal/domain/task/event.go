package task

// If business logic needs to handle any event,
// it should be define in the domain layer.
// The application layer should gets the event
// from domain layer and raise it to the outside world.
